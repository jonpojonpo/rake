# rake fast secure agent sandbox


LLM Agents are fantastic but prompt injection could make them dangerous on a host system


techstack 

rust wasmtime wasm sandbox  uutils ripgrep.

data goes in insights come out..

one of more files go in the ephemeral sandbox 1-40mb say an the agent repeatedly calls analysis tools or injects code into the sandbox until completed


