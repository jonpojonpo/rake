# rake FastAPI server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# AWS
boto3>=1.34.0

# rake Python SDK dependencies
# (rake_sdk is installed as a local package — see pyproject.toml)

# Document conversion (all optional — server degrades gracefully if missing)
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23
pdfminer.six>=20221105
